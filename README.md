Buildflow v64

Only PWA launch/loading behavior was changed:
- Added a Buildflow-branded in-document launch cover.
- Added a minimal standalone web app manifest.
- Added a service worker so repeat launches can use the cached app shell.
- Existing app UI and functionality remain unchanged.
